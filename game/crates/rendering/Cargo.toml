[package]
name = "bm_rendering"
version = "0.0.0"
edition = "2021"
license = "GPL-3.0-or-later"
publish = false

[dependencies]
bevy = { workspace = true }
bm_core = { path = "../core" }
bm_config = { path = "../config" }

[features]
default = []
headless = []
